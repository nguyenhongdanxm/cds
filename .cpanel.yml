---
deployment:
  tasks:
    - export DEPLOYPATH=/home/capnachi/cds.noitruxinman.edu.vn/
    - /bin/mkdir -p "$DEPLOYPATH/assets" "$DEPLOYPATH/includes"
    - /bin/cp -R assets/. "$DEPLOYPATH/assets/"
    - /bin/cp -R includes/. "$DEPLOYPATH/includes/"
    - /bin/cp -- *.php "$DEPLOYPATH"
